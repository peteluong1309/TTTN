import React from 'react';
// import PropTypes from 'prop-types';

const teacher = ({ user }) => {
  return (
    <div>
      <p>Giảng viên: {user.name}</p>
    </div>
  );
};



export default teacher;